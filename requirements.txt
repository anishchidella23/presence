# Face detection, recognition, landmarks and head pose.
# The buffalo_l pack bundles all four, so liveness geometry comes out of the
# same forward pass as the embedding - no second landmark library needed.
insightface==2.0
onnxruntime==1.30.0

# Imaging / math
opencv-python==5.0.0.93
numpy==2.5.3

# Web kiosk
fastapi==0.121.2
uvicorn[standard]==0.38.0
python-multipart==0.0.20
